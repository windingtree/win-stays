syntax = "proto3";

package videre.type;

import "contact.proto";

message Person {
  string first_name = 1;
  optional string middle_names = 2;
  string last_name = 3;
  optional string alternate_name = 4;

  repeated videre.type.Email emails = 5;
  repeated videre.type.PhoneNumber phones = 10;
}